{
  "name": "jettison-react",
  "private": true,
  "version": "0.1.0",
  "description": "Jettison — an enforced architecture for enterprise React applications, demonstrated by the Low Orbit Records label console. Any module can be thrown overboard and the app keeps flying; CI proves it on every push.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "echo 'The app scaffold has not landed yet' && exit 1",
    "type-check": "echo 'ok (no sources yet)'",
    "lint": "eslint . --ignore-pattern 'fixtures/src/**'",
    "test": "vitest run",
    "build": "echo 'ok (nothing to build yet)'"
  },
  "devDependencies": {
    "eslint": "^10.8.1",
    "eslint-import-resolver-typescript": "^4.4.5",
    "eslint-plugin-boundaries": "^7.2.0",
    "prettier": "^3.9.6",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.67.0",
    "vitest": "^4.1.10"
  }
}
