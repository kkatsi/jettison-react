{
  "name": "jettison-react",
  "private": true,
  "version": "0.1.0",
  "description": "Jettison — an enforced architecture for enterprise React applications, demonstrated by Mission Control. Any module can be thrown overboard and the app keeps flying; CI proves it on every push.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "echo 'App scaffold lands in Phase 2 — see IMPLEMENTATION.md' && exit 1",
    "type-check": "echo 'ok (no sources yet)'",
    "lint": "echo 'ok (eslint.config.js lands in Phase 1)'",
    "test": "echo 'ok (no tests yet)'",
    "build": "echo 'ok (nothing to build yet)'"
  }
}
