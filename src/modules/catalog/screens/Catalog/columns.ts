// The table's column widths, in one place because the header row and the body
// rows must agree. Grid on <tr> keeps the widths exact and the markup a real
// table — a catalogue is tabular data, whatever it is styled like.
export const COLUMNS = 'grid grid-cols-[44px_1fr_168px_92px_116px_116px_168px]';
