# Vendored, and kept byte-identical to upstream so `cmp -r` still measures the
# drift — formatting it would cost exactly what ADR-006 relies on.
tools/oxlint/anti-slop/

# Generated by `msw init`, vendored verbatim.
public/mockServiceWorker.js

dist/
coverage/
package-lock.json
